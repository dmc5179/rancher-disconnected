---

rancher_version: '2.4.5'
rke_version: '1.1.3'
arch: 'amd64'

mirror_base: '/tmp/rancher'

mirror_registry: 'localhost:5000'

pull_secret: '/run/user/1000/containers/auth.json'

rancher_config: '{{ mirror_base }}/cluster/cluster.yml'
